[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "spires-postprocess"
version = "0.1.0"
description = "SPIReS postprocessing: cloud gap-fill and tree masking/inpainting"
readme = { file = "README.md", content-type = "text/markdown" }
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Niklas Griessbaum" }]
keywords = ["snow", "remote sensing", "xarray", "cloud gap-fill", "inpainting"]
dependencies = [
    "spires-contract",
    "scipy",
    "torch",
    "lama",
]

[project.optional-dependencies]
test = ["pytest"]

[project.urls]
Homepage = "https://github.com/SPIReS-Organization/spires-postprocess"
Repository = "https://github.com/SPIReS-Organization/spires-postprocess"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
